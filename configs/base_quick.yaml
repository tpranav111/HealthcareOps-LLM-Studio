project:
  name: healthcare-llm-quick
  domain: healthcare
  seed: 42
paths:
  data_dir: data
  artifacts_dir: artifacts
  logs_dir: logs
models:
  base_model_path: "tpranav/HealthcareLLM"
  embedding_model_path: "sentence-transformers/all-MiniLM-L6-v2"
  reranker_model_path: ""
  tokenizer_path: "${models.base_model_path}"
runtime:
  device: cpu
  num_workers: 2
  max_seq_len: 32
  precision: fp32
  seed: 42
tracking:
  mlflow_uri: "file:./mlruns"
  experiment_name: "healthcare_llm_quick"
